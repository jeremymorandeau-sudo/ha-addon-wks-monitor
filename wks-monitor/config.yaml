name: "WKS Monitor"
version: "1.0.0"
slug: "wks_monitor"
description: "Lecture des onduleurs WKS/Voltronic en parallèle (QPGS0–2) et publication MQTT"
arch:
  - amd64
startup: application
boot: auto
uart: true
init: false
homeassistant_api: false

# 🧩 Image Docker hébergée sur GitHub (pour que HA puisse la télécharger automatiquement)
image: "ghcr.io/jeremymorandeau-sudo/wks-monitor-{arch}"

# ⚙️ Options par défaut (personnalisables plus tard via l’interface HA)
options: {}
schema: {}
